# Copy to .env (gitignored) and fill in real values for local dev.
# A spec never contains a literal key -- only a ${ENV_VAR} placeholder
# (packages/spec's `model.api_key`, PLAN.md "Secrets", ADR-0009).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
