# Real OpenRouter key (BYOK) -- required. Every model call in every
# mastra-projects demo goes through OpenRouter, never a direct
# ANTHROPIC_API_KEY (standing preference, not specific to this demo).
OPENROUTER_API_KEY=

# Which OpenRouter model to use. Defaults to openai/gpt-4o-mini if unset.
OPENROUTER_MODEL=openai/gpt-4o-mini

# Real Supabase project (pgvector enabled) storing the ingested document
# chunks and their embeddings. Project URL from Project Settings -> API.
SUPABASE_URL=https://cpirnzdlrhrvsuzqmbxt.supabase.co

# The project's real Secret API key (Project Settings -> API Keys ->
# Secret keys -- Supabase's current key system, format sb_secret_...;
# never the publishable/anon key). This process is the only consumer and
# always runs server-side; RLS is enabled with no policies on both tables,
# so only a secret key can read/write them at all. A legacy service_role
# key (under "Legacy API Keys") also works if that's what the project
# still shows -- createClient() accepts either.
SUPABASE_SECRET_KEY=

# Local port this server listens on.
PORT=8790
