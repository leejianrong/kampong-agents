// Local web canvas app (PLAN.md Shape S2, ADR-0005: local web app served by
// the CLI, not a desktop shell). Any UI built here uses Material Design 3
// (see the repo's agent brief / material-design-3 skill) — color roles, type
// scale, shape/elevation tokens, state layers — rather than ad hoc styling.
// Implementation lands with SLICES.md V1 — this file is scaffolding only, to
// prove the build/lint/test pipeline.

export const PACKAGE_NAME = "@kampong/canvas-app";
