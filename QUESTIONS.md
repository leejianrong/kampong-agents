# Questions

Statuses: `DECIDED` (user answered) · `ASSUMED` (default taken, correct it if wrong) · `FORK` (waiting on the user) · `DEFERRED` (not needed this milestone).

## Open forks

_(empty — round 1 closed all three)_

## Register

| ID  | Question                                                                                               | Status                | Answer or default                                                                                                                                                                                                            | Landed                         |
| --- | ------------------------------------------------------------------------------------------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |
| F1  | Single-agent or multi-agent org-chart scope for v1?                                                    | DECIDED               | Single-agent only for v1; spec schema keeps room for a `sub_agents` extension later.                                                                                                                                         | ADR-0001                       |
| F2  | Full bidirectional Python/TS sync, or YAML source of truth with one-way export?                        | DECIDED               | YAML is the single lossless source of truth, fully bidirectional with canvas. TypeScript export is one-way generated ("eject").                                                                                              | ADR-0002                       |
| F3  | Runtime framework: Mastra (TS) or LangGraph (Python)?                                                  | DECIDED               | Mastra — single-language (TypeScript) stack end to end.                                                                                                                                                                      | ADR-0003                       |
| Q4  | Who is the primary user, and who wins on conflict with secondary users?                                | ASSUMED               | Technical builder (developer/agency engineer) is primary; non-technical teammates are secondary viewers/editors of the same spec. On conflict, the canvas must never write YAML a developer wouldn't want in a diff.         | PLAN §Users and actors         |
| Q5  | Does MVP need to actually execute agents, or is design+export enough for v1?                           | ASSUMED               | MVP includes local execution. An unrunnable builder can't validate guardrails/HITL or serve as a dry-run sandbox.                                                                                                            | PLAN §Scope; SLICES V2         |
| Q6  | Is the canvas a desktop app (Electron/Tauri) or a local web app?                                       | ASSUMED               | Local web app, served by the CLI (`kampong dev` → localhost). Reuses the same UI for local and later hosted mode; no desktop packaging.                                                                                      | ADR-0005                       |
| Q7  | Does MVP need a separate LLM gateway (LiteLLM/Portkey)?                                                | ASSUMED               | No. Mastra's Vercel AI SDK layer already gives multi-provider model switching. A gateway with automatic cross-provider failover is a hosted-mode concern.                                                                    | ADR-0004                       |
| Q8  | What happens if a configured local model (Ollama) is unavailable?                                      | ASSUMED               | Clear error, never a silent fallback to a paid cloud API — avoids surprise spend and surprise data egress.                                                                                                                   | PLAN §Implementation decisions |
| Q9  | Where does canvas node layout (x/y position) live?                                                     | ASSUMED               | A sidecar file (`.kampong/layout.json`), not embedded in the YAML spec — keeps the spec hand-authorable and diff-clean.                                                                                                      | ADR-0006                       |
| Q10 | Two writers, one possibly external (a text editor): who wins?                                          | ASSUMED               | File-watch + reload prompt. The canvas detects an external change and offers to reload rather than silently overwriting. Real-time multi-user editing is a hosted-era concern.                                               | PLAN §Implementation decisions |
| Q11 | What does a human-approval (HITL) step actually do locally, with no backend?                           | ASSUMED               | A blocking interactive prompt — a browser modal during a canvas test run, or a CLI stdin prompt for headless `kampong run`. Slack/email escalation needs OAuth/webhook infra and is deferred.                                | PLAN §Shape S3; SLICES V2      |
| Q12 | Is tool definition natural-language-parsed or a structured form?                                       | ASSUMED               | A structured form (name, HTTP method, URL with `{placeholders}`, response-extraction path) is the primary, zero-LLM-calls path. NL-assisted drafting is an optional layer on top, not load-bearing.                          | PLAN §Affordances              |
| Q13 | Is "type a sentence, get a generated harness" (prompt-to-workflow) in scope?                           | DEFERRED              | No — a separate, nondeterministic R&D problem, not required to prove canvas-code duality.                                                                                                                                    | n/a                            |
| Q14 | Can secrets ever live inside the YAML spec?                                                            | ASSUMED               | No. API keys live in `.env`/OS keychain; the spec references `${ENV_VAR}` placeholders only, so any spec file is always safe to commit.                                                                                      | PLAN §Security                 |
| Q15 | Does the local tool phone home by default?                                                             | ASSUMED               | No telemetry leaves the machine by default in local mode — a trust/positioning decision for a "no lock-in" dev tool. Future analytics must be explicit opt-in.                                                               | PLAN §Security                 |
| Q16 | Does the spec need a version field from day one?                                                       | ASSUMED               | Yes — `version` field plus a simple field-based migration path, no elaborate migration tooling at MVP scale.                                                                                                                 | PLAN §Implementation decisions |
| Q17 | What is the storage model — files or a database?                                                       | ASSUMED               | Plain files: one YAML spec per agent + sidecar layout file, git-diffable by design. Optional local run-history log (SQLite or flat JSON); no database required for MVP.                                                      | PLAN §Shape S1                 |
| Q18 | How do we know the MVP actually works — stated so a test could check it?                               | ASSUMED               | See PLAN §Requirements and SLICES.md end-to-end lines — each is a checkable procedure, not a vibe.                                                                                                                           | PLAN §Testing approach         |
| Q19 | Are enterprise features (hosted/BYOK, SSO/RBAC/audit) cut from scope or roadmapped?                    | DECIDED (mid-session) | Roadmapped, not cut. Sequenced as explicit later slices after local-first + duality + export ship, per user direction.                                                                                                       | SLICES V5, V6                  |
| Q21 | When does a hosted multi-tenant SaaS mode arrive?                                                      | DEFERRED              | As roadmap slice V5, reusing the local web canvas UI.                                                                                                                                                                        | SLICES V5                      |
| Q22 | When does enterprise governance (SSO/SCIM/RBAC/PII scrubbing/audit logs/cost circuit breakers) arrive? | DEFERRED              | As roadmap slice V6, after hosted mode (V5) exists.                                                                                                                                                                          | SLICES V6                      |
| Q23 | When does an LLM gateway with automatic cross-provider failover arrive?                                | DEFERRED              | Folds into V5 (hosted), once reliability-at-scale matters.                                                                                                                                                                   | SLICES V5                      |
| Q24 | Is real-time multi-user collaborative editing in scope?                                                | DEFERRED              | No — single-user local-first for MVP and for the hosted slice's first cut; revisit only if customer pull demands it.                                                                                                         | n/a                            |
| Q25 | Frontend framework + canvas rendering library?                                                         | DECIDED (mid-session) | React + Vite + `@xyflow/react` (MIT) — the canvas is a node-graph editor, and this is the mature, purpose-built, de-risked choice (Langflow uses the same library for the same problem); no comparably mature Svelte option. | ADR-0007                       |
| Q26 | Local server framework + how run-progress streams to the canvas?                                       | DECIDED (mid-session) | Fastify serving static canvas assets + a spec-CRUD REST API; run-progress streams via Server-Sent Events (server→client is the dominant flow; approve/reject is a plain POST).                                               | ADR-0007                       |
| Q27 | Which YAML parsing library?                                                                            | DECIDED (mid-session) | The `yaml` package (eemeli/yaml), not `js-yaml` — preserves comments/formatting across parse→mutate→stringify.                                                                                                               | ADR-0007                       |
| Q28 | Is hand-editing YAML in an external IDE/agentic coding tool a primary workflow or an edge case?        | DECIDED (mid-session) | Primary, first-class workflow. Auto-reload on external change by default (no prompt); prompt only on a genuine in-flight-mutation conflict.                                                                                  | ADR-0008                       |
| Q29 | Should the `AgentSpec` schema be published for external editor/agentic-tool consumption?               | DECIDED (mid-session) | Yes — a standalone JSON Schema, versioned with the spec, referenced via the `yaml-language-server` pragma.                                                                                                                   | ADR-0008, PLAN §Shape S7       |

## Coverage

| Category                 | Covered by                 |
| ------------------------ | -------------------------- |
| Primary user and actors  | Q4                         |
| Scope boundary           | F1, Q19, Q21, Q22          |
| Data model and identity  | F2, Q16, Q17               |
| State and storage        | Q17, Q9                    |
| Concurrency and conflict | Q10, Q28                   |
| Interfaces and contracts | Q6, F3, Q25, Q26, Q29      |
| Failure behaviour        | Q8, Q11                    |
| External dependencies    | F3, Q7, Q23, Q25, Q26, Q27 |
| Runtime and deployment   | Q6, Q5                     |
| Measurable success       | Q18                        |
| Security and secrets     | Q14, Q15                   |
| Versioning and migration | Q16                        |
