{
  "name": "@kampong/spec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p .",
    "generate:schema": "node scripts/generate-schema.mjs"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
