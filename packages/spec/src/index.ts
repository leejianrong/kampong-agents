// AgentSpec schema + validator (PLAN.md Shape S1, ADR-0002: YAML is the
// single lossless source of truth). Implementation lands with SLICES.md V1 —
// this file is scaffolding only, to prove the build/lint/test pipeline.

export const PACKAGE_NAME = "@kampong/spec";
