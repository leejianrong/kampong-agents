// Mastra-backed execution engine (PLAN.md Shape S3/S4, ADR-0003, ADR-0004):
// AgentSpec -> Mastra agent, guardrail/HITL blocking approval, mock/record
// tool layer, Ollama adapter. Implementation lands with SLICES.md V2/V3 —
// this file is scaffolding only, to prove the build/lint/test pipeline.

export const PACKAGE_NAME = "@kampong/engine";
